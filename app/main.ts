import { bootstrap } from 'angular2/platform/browser'
import { AppComponent } from './app.component'

bootstrap(AppComponent);

//when we bootstrap our root component we are loading our entire app and indicating to Angular that this is the entry point to our application that will load the rest of it for us//
